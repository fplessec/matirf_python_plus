"""
benchmarks — comparing the reconstruction methods, safely and reproducibly.

    runner.py    the resumable, crash-proof campaign executor (read its docstring first)
    _tests.py    python -m benchmarks._tests
"""
